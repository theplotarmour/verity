/**
 * Placeholder root. The platform foundation is server-side; no product UI is
 * built during this phase by design.
 */
export default function Page() {
  return <main>Verity platform foundation</main>;
}
