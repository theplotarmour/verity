// Stub for the `server-only` marker, aliased in vitest.config.ts.
// The real module throws on import outside a Next bundle; the build-time guard
// it provides is enforced by Next's bundler, not by the test runner.
export {};
